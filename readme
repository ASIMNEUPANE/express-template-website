// get -database batw data lignu
// post - to give new data to database
// put - to update data in database
// delete - to delete data in database



//  router.post("/cc", async (req, res,next) => {
    // try{
//   const { from, to, amount } = req.body;
//   console.log(req.body);
//   const currencyConverter = new cc({
//     from: from,
//     to: to,
//     amount: Number(amount),
//   });
//   const convo = await currencyConverter.convert();
//   res.send(`<h1>${convo} </h1>`);
// });